server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MiPf0/Sensorrecords_Backend/cloud-config-server-configuration-repo
          clone-on-start: true
  profiles:
    active: native